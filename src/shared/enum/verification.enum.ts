export enum VerificationType {
  Otp = "otp",
  Email = "email",
}

export enum VerificationBehavior {
  FindEmail = "find_email",
  ResetPassword = "reset_password",
  RegisterAccount = "register_account",
}
